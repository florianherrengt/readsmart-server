// @flow
export const schema = [
    `
    type IotConnection {
        iotEndpoint: String
        region: String
        accessKeyId: String
        secretKey: String
        sessionToken: String
    }
`
];
