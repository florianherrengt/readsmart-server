machine:
  node:
    version: 6.1.0

dependencies:
  override:
    - npm i -g yarn
    - yarn
    - ./node_modules/.bin/flow-typed install

test:
  override:
    - yarn run flow

deployment:
  branch: master
  commands:
    - yarn run deploy:dev